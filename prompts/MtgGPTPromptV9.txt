##### MtgGPTPrompt v9  (clean A + Quick Stats B) #####
IGNORE ALL EARLIER CHATS.  THIS FILE IS YOUR ONLY INSTRUCTION SET.

You will always receive exactly two plaintext files:
  1️⃣  This prompt file (MtgGPTPromptV9.txt)
  2️⃣  One meeting transcript (e.g. dinnerTranscript.txt)

Run the following three‑stage workflow and STOP.  
Exactly THREE pauses are allowed:
  • After Stage A  ➜  === END STAGE A (type "y" to continue) ===
  • After Stage B  ➜  === END STAGE B (type "y" to append stats) ===
  • After Stage C  ➜  === END STAGE C (type "y" to finalize and display) ===

────────────────────────────────────────────────────────────
STAGE A — NARRATIVE SUMMARY (clean bullets)
────────────────────────────────────────────────────────────
Goal: craft a fluent, investor‑style pitch for **each company**.

Header line (bold):
  **(TICKER) — Long / Short — mm/dd/yyyy — $price**

• Keep adding primary bullets until every material idea is voiced
  (edge, catalysts, valuation maths, debate, risks, colour).  
• One idea per bullet; natural prose.  
• If a bullet contains multiple ideas (joined by “;”, “ and ”, or “ but ”), split into
  separate bullets.  If the second idea depends on the first, indent it one level.
• Push ALL numbers / % / $ / dates to indented sub‑bullets.

Print every company’s section, then:
=== END STAGE A (type "y" to continue) ===

────────────────────────────────────────────────────────────
STAGE B — FACT LEDGER (Quick‑stats source)
────────────────────────────────────────────────────────────
Goal: capture **all** remaining stats / metrics / quotes, grouped by company.

Loop over every ticker seen in Stage A:

  (TICKER) — Facts
  ──────────────────────
  • Create whatever buckets are needed (Financial, Edge/Tech, AI Use Cases, …).
  • Unlimited nesting — NOTHING is dropped.
  • Do *not* rewrite or merge with Stage A.

After the final company:
=== END STAGE B (type "y" to append stats) ===

────────────────────────────────────────────────────────────
STAGE C — BUILD FINAL OUTPUT (clean + append stats)
────────────────────────────────────────────────────────────
For each company **in the same order as Stage A**:

1. **Final A cleanup**  
   • Re‑scan every Stage A bullet you produced.  
   • If a bullet still contains multiple ideas joined by “;”, “ and ”, or “ but ”:  
     – Split at that join‑word.  
     – Keep the first idea as the parent bullet.  
     – Indent the remaining idea(s) exactly one level deeper.  
   • _Do not reorder, merge, or delete any content._ Only split & indent.

2. **Print the Stage A header and its cleaned bullet tree.**

3. **Append Quick‑Stats / Metrics**  
   ```
   • Quick Stats / Metrics
       •  [PASTE the ENTIRE Stage B block for this company, exactly as produced — headers, bullets, indentation unchanged]
   ```
4. Repeat for the next company.

After the last company:  
• Wrap everything in dark‑mode HTML (CSS from previous prompts).  
• Save via:

```python
from pathlib import Path
Path("/mnt/data/InvestmentSummaryV9.html").write_text(final_html, encoding="utf-8")
```

• Then print:  
`[Download the summary](sandbox:/mnt/data/InvestmentSummaryV9.html)`

=== END STAGE C (type "y" to finalize and display) ===
